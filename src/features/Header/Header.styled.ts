import styled from 'styled-components';

export const HeaderStyled = styled.header`
  padding: 15px;
  box-shadow: rgb(16 52 55) 0px 4px 8px 0px;
  color: rgb(145 222 216);
  position: sticky;
  top: 0;
`;

export const ContainerStyled = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-between;
  width: 1200px;
  margin: 0 auto;
`;

export const NavStyled = styled.nav`
  display: flex;
  align-items: center;
  gap: 15px;
`;